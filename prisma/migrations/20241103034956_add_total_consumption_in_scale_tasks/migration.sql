BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[scale_tasks] ADD [total_consumption] DECIMAL(32,16) NOT NULL CONSTRAINT [scale_tasks_total_consumption_df] DEFAULT 0.0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
